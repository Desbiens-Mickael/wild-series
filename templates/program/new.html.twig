{% extends "base.html.twig" %}

{% block title %}Ajout d'une nouvelle série{% endblock %}

{% block body %}
    <div class="container">
        {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-outline-primary text-font-colour">Ajouter la série</button>
        {{ form_end(form) }}
    </div>
{% endblock %}